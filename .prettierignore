dist/
coverage/
node_modules/
package-lock.json

# Rust build output. Only present after a local `npm run desktop:build` -- CI
# checks out fresh and never sees it -- and it holds generated JS and compressed
# asset blobs that Prettier tries, and fails, to parse.
src-tauri/target/

# Local-only agent scratch space; holds git worktrees for other sessions.
.claude/
